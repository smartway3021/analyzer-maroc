import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
})

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: 'AI Tender Analyzer Maroc - Analyse Intelligente d\'Appels d\'Offres',
  description:
    'Plateforme SaaS intelligente pour analyser et répondre aux appels d\'offres marocains avec l\'intelligence artificielle.',
  keywords: ['appel d\'offres', 'maroc', 'IA', 'analyse', 'soumission', 'marchés publics'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html
      lang="fr"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full">
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}
